{{#sprites}}
@define-mixin $sprite sp_{{name}} {
    background-image: url(../images/sprites/{{{escaped_image}}});
    background-position: {{px.offset_x}} {{px.offset_y}};
    /**
    width: {{px.width}};
    height: {{px.height}};
    */
}
{{/sprites}}
